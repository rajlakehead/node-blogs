nums.sort()
        res = (nums[0][1] - nums[0][0]) + 1
        currmax = nums[0][1]
        
        for i in range(1, len(nums)):
            if nums[i][0] > currmax:
                res += nums[i][1] - nums[i][0] + 1
                currmax = nums[i][0]
            elif nums[i][1] > currmax:
                res += nums[i][1] - nums[i - 1][1] 
                currmax = nums[i][1]
            else:
                continue
        return res